# Nginx: ProductB2B (catalog) – optional public exposure.
# Replace products.yourdomain.com with your domain.
# Copy to /etc/nginx/sites-available/b2b-products, then:
#   sudo ln -s /etc/nginx/sites-available/b2b-products /etc/nginx/sites-enabled
#   sudo nginx -t && sudo systemctl reload nginx
# SSL: certbot --nginx -d products.yourdomain.com

server {
    listen 80;
    server_name products.yourdomain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name products.yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/products.yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/products.yourdomain.com/privkey.pem;

    location / {
        include proxy_params;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
