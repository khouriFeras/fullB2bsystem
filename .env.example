# B2B stack – copy to .env and fill in real values.
# Used by docker-compose for postgres, productb2b, orderb2bapi.
# NEVER commit .env to git.

# --- Compose / Server ---
# Host port for OrderB2bAPI (default 8081). Inside containers orderb2bapi uses 8080.
PORT=8081

# --- Database (postgres container + OrderB2bAPI) ---
# Compose uses DB_PASSWORD and DB_NAME for the postgres container.
# OrderB2bAPI gets DB_HOST=postgres and DB_PORT=5432 from compose; other vars from here.
DB_USER=postgres
DB_PASSWORD=123123
DB_NAME=b2bapi
DB_SSLMODE=disable

# --- OrderB2bAPI (optional; compose overrides DB_HOST, PORT, PRODUCT_B2B_URL, GET_DELIVERY_STATUS_URL) ---
ENVIRONMENT=production
LOG_LEVEL=info

# --- Shopify ---
# Example: your-store.myshopify.com (no https://)
SHOPIFY_SHOP_DOMAIN=
# Admin API access token (starts with shpat_). Also used as SHOPIFY_ADMIN_TOKEN for ProductB2B.
SHOPIFY_ACCESS_TOKEN=

# --- ProductB2B (server-to-server) ---
# Must match the key ProductB2B expects. Compose sets PRODUCT_B2B_URL for orderb2bapi.
PRODUCT_B2B_SERVICE_API_KEY=
# Public HTTPS URL for ProductB2B (Shopify webhook registration). Example: https://products.jafarshop.com
PUBLIC_BASE_URL=

# --- API security ---
# Use a strong random value in production.
API_KEY_HASH_SALT=default-salt-change-in-production
