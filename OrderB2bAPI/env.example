## Copy this file to `.env` and fill in real values.
## NEVER commit `.env` to git. This repository ignores `.env` via `.gitignore`.

# Server
PORT=8080
ENVIRONMENT=development
LOG_LEVEL=info

# Database
# When using Docker: API uses postgres:5432; from host use DB_HOST=127.0.0.1 DB_PORT=5434 to create partners in same DB
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=b2bapi
DB_SSLMODE=disable

# Shopify
# Example format: your-store-name.myshopify.com (no https://)
SHOPIFY_SHOP_DOMAIN=
# Admin API access token (starts with shpat_)
SHOPIFY_ACCESS_TOKEN=

# ProductB2B (catalog and product details)
# Base URL of ProductB2B service (e.g. http://productb2b:3000 or http://localhost:3000)
PRODUCT_B2B_URL=
# Service API key for server-to-server calls (set PRODUCT_B2B_SERVICE_API_KEY in ProductB2B .env)
PRODUCT_B2B_SERVICE_API_KEY=

# API
# Change in production.
API_KEY_HASH_SALT=default-salt-change-in-production

